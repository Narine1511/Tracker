//
//  AnalyticsViewController.swift
//  Tracker
//
//  Created by Наринэ  Овсепян on 14.08.2026.
//

/*import UIKit
final class AnalyticsViewController: ViewController {
    private func updatePlaceholderVisibilityFornalyticsVC() {
        let hasTrackers = !filteredCategories.isEmpty && filteredCategories.contains { !$0.trackers.isEmpty }
        if hasTrackers || currentFilter != "Все трекеры" {
            placeholderImageView.isHidden = true
            placeholderLabel.isHidden = true
            return
        }
        // ✅ НЕТ ТРЕКЕРОВ ВООБЩЕ
        placeholderImageView.isHidden = false
        placeholderLabel.isHidden = false
        placeholderLabel.text = "Что будем отслеживать?"
        placeholderImageView.image = UIImage(named: "star")
        
        updateFilterButtonAppearance()
    }
}*/
